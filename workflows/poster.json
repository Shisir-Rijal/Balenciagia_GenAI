{
  "_SETUP": [
    "HOW TO WIRE THIS UP (vetements_fw24_comfy_cloud_master_v2_4):",
    "",
    "The workflow already handles all 3 backgrounds in parallel — no changes needed",
    "for background selection. The kiosk picks the right output automatically.",
    "",
    "ONE THING TO CHANGE in the exported API-format JSON:",
    "  Node 11 (INPUT 2 — Frontal Portrait Identity Reference) → set image to __INPUT_IMAGE__",
    "  This is the guest's face photo. Everything else stays as-is.",
    "",
    "STEPS:",
    "1. Open vetements_fw24_comfy_cloud_master_v2_4 in ComfyUI",
    "2. Settings → Enable Dev Mode → Save (API Format)",
    "3. Replace everything in this file with the exported JSON",
    "4. Find node '11' and set: \"image\": \"__INPUT_IMAGE__\"",
    "5. Set COMFY_URL=http://localhost:8188 in .env if ComfyUI runs on a different port",
    "",
    "OUTPUT MAPPING (handled automatically by the kiosk):",
    "  env1 → 05A_FINAL  (Background A)",
    "  env2 → 05B_FINAL  (Background B)",
    "  env3 → 05C_FINAL  (Background C)"
  ],
  "11": {
    "class_type": "LoadImage",
    "inputs": { "image": "__INPUT_IMAGE__", "upload": "image" }
  }
}
